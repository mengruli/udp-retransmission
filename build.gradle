allprojects {
    repositories {
        jcenter()
    }
}

subprojects {
    version = '1.0'
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    jar {
        manifest.attributes provider: 'gradle'
    }
}